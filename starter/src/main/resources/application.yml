# 开发阶段导入，应用阶段移除

server:
  port: 8888
spring:
  # hikariPool
  datasource:
    username: root
    password: Duxiaoyu@123
    url: jdbc:mysql://139.224.81.29/mybatis_rabbit_plug?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowMultiQueries=true

rabbit-mybatis-plug:
  banner: true
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.rabbit.core.entity